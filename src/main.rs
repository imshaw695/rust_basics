fn main() {
    let mut x = 4;  // mut is short for mutable, you must delcare a variable as mutable if you want to change it later on.
    println!("x is {}", x);
    x = 5;
    println!("x is {}", x);
}
