{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "Debug",
          "type": "lldb",
          "request": "launch",
          "program": "${workspaceFolder}/target/debug/rust_basics.exe",
          "args": [],
          "cwd": "${workspaceFolder}",
          "preLaunchTask": "cargo build"
      }
  ]
}
